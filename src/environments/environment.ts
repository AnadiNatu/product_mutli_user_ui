// =============================================================================
// frontend/src/environments/environment.ts
// PROFILE: local development (ng serve)
// =============================================================================

export const environment = {
  production: false,
  // Local: Angular dev server proxies /api to localhost:8080
  apiBaseUrl: 'http://localhost:8083'
};
